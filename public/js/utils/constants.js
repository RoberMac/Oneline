export const TIMELINE_SCROLL = {
    target: '.scrollTo--target',
    container: '.oneline__wrapper',
    duration: 700,
};

export const MIN_EXTRACT_COUNT = 20;
export const MAX_SHOWING_COUNT = 100;
